<!DOCTYPE html>
<html lang="en">
<body>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Phone Number</th>
                <th>Email</th>
                <th>Address</th>
                <th>City</th>
                <th>Date of Birth</th>
                <th>Gender</th>
            </tr>
        </thead>
    </table>
    <form action="/pages/:pageNumber" method="get">
        <input type="text" value="<%= currentPage %>">
        <input type="submit" value="Search">
    </form>
    <input type="button" value="Next">
    <script>
        function handleOnChange(){
            updatePageNumber()
        }
        function updatePageNumber(){

        }

        document.addEventListener('submit', (event) => {
            event.preventDefault();

            function goto(pageNumber){

            }
        })
    </script>
</body>
</html>